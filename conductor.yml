name: TestProject
services:
  - name: mysql
    container: dev-mysql
components:
  - name: test
    tags:
      - test
    path: ./
    retry: false
    services:
      - mysql
    start:
      command: ls
  - name: long
    path: ./
    tags:
    - long
    retry: false
    start:
      command: sleep
      args:
        - 5